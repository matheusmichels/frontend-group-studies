import { sum } from '@calculator-dos-guri/sum';
import { multiply } from '@calculator-dos-guri/multiply';
import { subtract } from '@calculator-dos-guri/subtract';
import { divide } from '@calculator-dos-guri/divide';

console.log(sum(3, 5));
console.log(multiply(3, 5));
console.log(subtract(3, 5));
console.log(divide(3, 5));
